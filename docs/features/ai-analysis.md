# AI-Powered Analysis

> AI analysis integrates multiple large language models to provide intelligent metrics analysis and alert content generation.

---

## Overview

Gateway supports the following AI providers:

| Provider | Models | Region |
|----------|--------|--------|
| **OpenAI** | GPT-4, GPT-3.5-turbo | Overseas |
| **Anthropic** | Claude 3 | Overseas |
| **Qwen** | qwen-plus, qwen-turbo | Domestic (China) |
| **DeepSeek** | deepseek-chat | Domestic (China) |
| **Ollama** | llama2, mistral | Local |

---

## Features

| Feature | Description |
|---------|-------------|
| **Metrics Analysis** | Analyze current metrics, identify anomalies |
| **Alert Generation** | AI-generated alert content and recommendations |
| **Trend Prediction** | Predict resource demand trends |
| **Anomaly Detection** | Detect abnormal traffic patterns |

---

## Configuration

```json
{
  "provider": "qwen",
  "apiKey": "your-api-key",
  "model": "qwen-plus",
  "baseUrl": "https://api.qwen.ai",
  "language": "zh"
}
```

| Parameter | Description |
|-----------|-------------|
| `provider` | AI provider name |
| `apiKey` | API key (encrypted storage) |
| `model` | Model name |
| `baseUrl` | Custom API endpoint (optional) |
| `language` | Output language (zh/en) |

---

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/api/ai/providers` | List available providers |
| `GET` | `/api/ai/providers/{provider}/models` | Get available models |
| `POST` | `/api/ai/validate` | Validate API key |
| `POST` | `/api/ai/config` | Save AI configuration |
| `POST` | `/api/ai/analyze` | Analyze metrics |

### Analyze Metrics

```bash
curl -X POST http://localhost:9090/api/ai/analyze \
  -H "Content-Type: application/json" \
  -d '{
    "metrics": {...},
    "language": "zh"
  }'
```

Response:
```json
{
  "analysis": "Current system status is overall healthy...",
  "recommendations": [
    "Recommend increasing Redis memory allocation",
    "Consider enabling caching for high-frequency routes"
  ],
  "alerts": []
}
```

---

## AI Alert Content

Alert email content is generated by AI:

```
Subject: [WARNING] Gateway CPU usage too high

Body:
Current Gateway CPU usage has reached 85%, exceeding the set threshold (80%).

Impact Analysis:
1. May cause increased request processing latency
2. Potential request timeouts during peak hours

Recommended Actions:
1. Check for abnormal traffic sources
2. Consider adding Gateway instances
3. Optimize routes with high resource consumption

Click here to view detailed monitoring data...
```

---

## Context Building

Context is built during AI analysis:

```
Metrics Analysis Context:
┌─────────────────────────────────────────────┐
│  - Gateway architecture description          │
│  - Current metrics (CPU, memory, QPS)        │
│  - Historical data trends                    │
│  - Route configuration                       │
│  - Recent error patterns                     │
└─────────────────────────────────────────────┘
```

---

## Best Practices

1. **Choose Appropriate Provider**: Use Qwen/DeepSeek for China, OpenAI/Claude for overseas
2. **Local Deployment**: Use Ollama local models for sensitive scenarios
3. **API Key Security**: Rotate regularly, limit permissions
4. **Regular Analysis**: Set up scheduled AI analysis tasks
5. **Combine with Monitoring**: Use AI analysis as a complement to monitoring

---

## Related Features

- [Monitoring & Alerts](monitoring-alerts.md) - Monitoring data source
- [Email Notifications](email-notifications.md) - AI-generated email content
- [AI Copilot Assistant](ai-copilot.md) - AI configuration assistant